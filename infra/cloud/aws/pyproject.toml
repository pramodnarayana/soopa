[project]
name = "infra-aws"
version = "0.1.0"
description = "AWS Infrastructure using Pulumi"
requires-python = ">=3.11"
dependencies = [
    "pulumi>=3.258.0,<4.0.0",
    "pulumi-aws>=6.83.4,<7.0.0",
    "pulumi-policy>=1.15.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.5",
]
