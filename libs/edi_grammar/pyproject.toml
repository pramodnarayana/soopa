[project]
name = "edi-grammar"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Pramod", email = "pramod.narayana@gmail.com" }
]
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["uv_build>=0.11.24,<0.12.0"]
build-backend = "uv_build"
