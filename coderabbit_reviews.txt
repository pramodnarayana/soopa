In
`@apps/edi/apps/edi-dp-jobs-worker/src/edi_dp_jobs_worker/adapters/outbound/database/postgres_edi_data_plane_outbox_repository.py`:
- Line 165: Update the event lookup in mark_completed/mark_failed to use
_active_events_shard_map.get() instead of removing the entry before the database
operation, then remove the mapping only after session.execute() and
session.commit() complete successfully. Preserve the existing missing-mapping
warning and return behavior.

In
`@apps/edi/packages/edi/src/edi/application/use_cases/pipeline/delivery_use_case.py`:
- Around line 61-66: Separate the `router.deliver` exception handling from
success bookkeeping in the delivery use case: keep `mark_delivery_failure` only
for exceptions raised by `router.deliver`, then perform `mark_delivery_success`
and its commit afterward so bookkeeping failures do not mark the completed
delivery as failed. Preserve the existing lease for reconciliation when success
bookkeeping fails.

In `@apps/edi/packages/edi/src/edi/config/settings.py`:
- Around line 120-124: Update the AppSettings model validator
validate_external_url to reject allow_private_ips when env is not "development",
raising a clear ValueError; preserve the existing external-URL validation for
all environments.

In `@apps/edi/packages/edi/src/edi/core/pipeline/delivery/as2.py`:
- Around line 45-48: Update the retryable error branches in the AS2 delivery
flow, including the paths around _emit_delivery_completed and the additional
referenced branches, so transient failures leave the message pending and do not
emit DELIVERY_COMPLETED before raising RuntimeError. Only terminal failures
should update the status to MessageStatus.FAILED, emit completion, and stop
retrying.

In
`@core/platform/apps/identity-jobs-worker/tests/integration/test_identity_outbox_worker_integration_structural.py`:
- Around line 18-22: Replace the private handlers-map assertions in the
structural integration test with calls to the public
container.jobs_consumer.handler using one message for each IdentityJobName.
Await each dispatch and assert the corresponding execute test double was called,
verifying the job_name-to-callback mapping through MessageDispatcher.dispatch.

In `@docker-compose.yml`:
- Line 80: Update the unified-api port mapping to bind host port 3000
specifically to 127.0.0.1 while retaining container port 8000, so the documented
local-development endpoints are not exposed on other host interfaces.

In `@infra/cloud/aws/packages/seedwork/ecs.py`:
- Around line 150-151: Complete the FireLens credential flow around the ECS
task-definition construction: export the OpenObserve secret ARNs, require and
pass both ARN parameters through every FireLens caller, grant each execution
role permission to read those secrets, and validate that the FireLens endpoint
and credential ARNs are present before creating the task definition; do not emit
empty valueFrom entries.

In `@infra/cloud/aws/platform/__main__.py`:
- Line 266: Require the ACM certificate in the `provision_alb` call so
`main_listener` uses HTTPS, remove the separate `zitadel_listener` resource, and
update `provision_target_group_and_rule` to use `main_listener.arn` as its
listener ARN.

In `@infra/cloud/aws/policy/__main__.py`:
- Around line 62-63: Update check_sg_ingress to read both cidrBlocks and
ipv6CidrBlocks, and apply the existing unrestricted-ingress restrictions when
either list contains 0.0.0.0/0 or ::/0. Preserve the current all-protocol, SSH,
and PostgreSQL validation behavior for IPv4 while extending it equivalently to
IPv6.

In `@infra/local/localstack-setup.sh`:
- Line 110: Update the localstack setup to attach EDI_CONFIG_ARN to
EDI_EVENTS_TOPIC_ARN with a filter matching the edi. event prefix, ensuring
EdiEventType control-plane events such as edi.as2_partner.created reach the
queue while preserving existing pipeline subscriptions.

In `@apps/edi/packages/edi/src/edi/config/settings.py`:
- Around line 126-128: Update the AppSettings.database field default_factory to
instantiate PlatformDatabaseSettings directly instead of returning a cast empty
dictionary, ensuring nested environment variables such as
SHARD_OVERRIDES__EDI_SHARD_1 are parsed during settings initialization.

In `@infra/cloud/aws/apps/edi/compute.py`:
- Around line 138-177: Update provision_compute to create control-plane and
data-plane jobs services using the edi_cp_jobs_worker.main and
edi_dp_jobs_worker.main entrypoints, respectively, via make_service. Include
both services in the returned services mapping alongside the existing AS2 and
worker services.

In `@infra/cloud/aws/apps/scheduler/__main__.py`:
- Around line 73-88: Provision and export the Identity jobs queue URL from the
identity stack and export the existing UCP jobs_q URL from the UCP stack.
Require both outputs in the scheduler stack and add them to the scheduler_worker
environment_vars as SQS_IDENTITY_JOBS_QUEUE_URL and SQS_UCP_JOBS_QUEUE_URL
alongside the existing queue mappings.
