Verify each finding against current code. Fix only still-valid issues, skip the
rest with a brief reason, keep changes minimal, and validate.

Inline comments:
In @.agents/AGENTS.md:
- Line 33: Update the “Platform Architecture Paradigms (Core Tenets)” Markdown
heading by adding exactly one blank line before and after it to satisfy
markdownlint MD022.

In `@apps/edi/apps/edi-worker/orchestrator/src/worker/jobs/outbox_sweeper.py`:
- Around line 78-80: Update the event filter in the outbox sweeper to derive
allowed event types from PIPELINE_EVENT_ROUTING_MAP rather than concatenating
PipelineEventType and NotificationEventType lists. Preserve selection and
dispatch alignment, and remove those enum imports if they become unused.

In `@apps/edi/packages/domain/src/domain/events.py`:
- Line 73: Provision the queue identified by the PRIORITY_NOTIFICATIONS_QUEUE
enum value, sqs-priority-notifications-queue, in the tracked infrastructure, or
update the enum and notification consumer to use an already provisioned queue
name. Ensure the resulting queue name is available to both publish_batch and
get_queue_url.

In `@apps/edi/packages/ui/src/routes/tenant.tsx`:
- Around line 215-219: Update the notification flow around NotificationBell and
useNotifications so queries are enabled only when tenantId, userId, and
accessToken are all present. Include an accessToken presence check in the query
guard, or render NotificationBell only when all required values exist,
preventing requests with an undefined bearer token.

In `@core/packages/ui/src/features/notifications/api/useNotifications.ts`:
- Around line 30-58: Update the useEffect dependency array in the notifications
stream setup to include accessToken, ensuring the EventSource is recreated with
the current token after silent renewals while preserving the existing cleanup
behavior.
- Around line 12-19: Update getBaseUrl so the localhost notification endpoint is
used only in development builds; when VITE_UCP_API_URL is missing in production,
throw a clear configuration error instead of returning the hardcoded localhost
URL. Preserve the existing URL normalization and endpoint suffix behavior when
the variable is present.
- Around line 37-53: Add an EventSource onerror handler alongside the existing
onmessage setup to log the stream failure and invalidate the notifications query
keyed by tenantId and userId through queryClient, allowing the REST endpoint to
refresh the list.
- Around line 33-35: The EventSource construction in useNotifications must stop
placing accessToken in the stream URL. Authenticate the SSE connection with a
short-lived single-use ticket, HttpOnly cookie, or an Authorization-header-based
fetch approach, while retaining tenant_id in the request for tenant middleware
compatibility.

In `@core/packages/ui/src/features/notifications/components/NotificationBell.tsx`:
- Around line 53-62: Update the notification content branch in NotificationBell
to check the useNotifications query error state before evaluating
notifications.length === 0, rendering a distinct failure message when loading
fails and retaining the empty-state message only for successful queries with no
notifications.

In
`@core/packages/ui/src/features/notifications/components/NotificationPreferencesPage.tsx`:
- Around line 87-121: Associate both form labels with their controls in the
notification preferences form: add a stable id to the Event Type input and
reference it from the label, then replace the Delivery Channels label/container
structure with a fieldset and legend or otherwise provide an accessible group
label for the channel buttons. Preserve the existing selection and toggle
behavior in ALL_CHANNELS.

In
`@core/packages/ui/src/features/notifications/components/NotificationsPage.tsx`:
- Around line 51-65: Add severity to the notification ORM model and
NotificationDTO, populate it in the REST mapping and SSE payload, and propagate
the actual value to the UI. Update NotificationsPage’s enriched row type to
include severity, preserve info only as the fallback for missing values, and
remove the any casts from severity access.

In
`@core/packages/ui/src/features/notifications/components/NotificationTemplatesPage.tsx`:
- Around line 316-321: Update handleDelete for NotificationTemplate to open the
existing UI dialog from components/ui/dialog and only call remove.mutate after
the user confirms deletion. Wire the Trash button’s flow to provide the selected
template to the dialog, with cancel leaving the template unchanged and
confirmation preserving the existing success and error toasts.
- Around line 404-410: Add a key to the TemplateEditorPanel instance in the
editing render block, derived from the edited template’s stable identity and
using a distinct value for the new-template case, so switching between templates
remounts the panel and reinitializes its editor state.
- Around line 79-117: Update the notification preview flow to destructure the
stable mutate function from usePreviewTemplate(ctx), then call that function
inside runPreview instead of the preview object. Replace preview with mutate in
runPreview’s dependency array so schedulePreview and the effect do not
reschedule previews on mutation state changes.

In `@core/platform/apps/notification_engine/pyproject.toml`:
- Around line 8-16: Remove the unused sse-starlette dependency from the project
dependency list in pyproject.toml, leaving the surrounding dependencies
unchanged.

In
`@core/platform/apps/notification_engine/src/notification_engine/adapters/inbound/postgres_listener.py`:
- Around line 84-90: Update the notification payload handling around
NotificationDTO construction to validate id, title, and body alongside tenant_id
and user_id before constructing the DTO. Use consistent optional-safe access and
reject/log missing required fields with the existing validation path, avoiding
direct subscripting that raises KeyError.
- Around line 52-69: Update _run_loop so every reconnect attempt is delayed by
the existing backoff interval, including when the inner connection-keepalive
loop exits because the connection closes cleanly. Preserve the current
cancellation handling and avoid adding duplicate or unnecessary delays for a
single reconnect cycle.
- Around line 40-50: Update PostgresNotificationListener.stop to cancel
self._task first and await its completion, handling only the expected
cancellation outcome so shutdown does not leave _run_loop pending. Then remove
the broad silent exception handling around remove_listener and close, allowing
connection-cleanup failures to propagate while preserving the final stop log.
- Line 26: Update the DSN normalization in the listener initialization to handle
any `postgresql+<driver>://` prefix, while preserving already-compatible
`postgres://` and `postgresql://` URLs. Add the proposed `re` import and use it
in the `database_url` transformation so `asyncpg.connect` receives a driver-free
PostgreSQL DSN.

In
`@core/platform/apps/notification_engine/src/notification_engine/adapters/inbound/sqs_poller.py`:
- Around line 12-16: Update the processor_func annotation in poll_sqs_queue to
require an awaitable return value, using the appropriate Awaitable or Coroutine
typing while preserving its dictionary argument type. Keep the existing await
processor_func call consistent with the updated contract.
- Around line 18-21: Update the client configuration in the SQS poller to derive
region_name from the configured AWS region, using the standard AWS environment
resolution as the fallback instead of hardcoding "us-east-1". Preserve the
existing aws_endpoint override and client_kwargs behavior.

In
`@core/platform/apps/notification_engine/src/notification_engine/adapters/outbound/postgres_in_app_persistence.py`:
- Around line 34-46: In the notification creation flow, add a warning log when
`user_ids` is empty after resolving recipients, specifically for the absent
`target_user_id` path with no active admin or owner. Log enough context to
diagnose the tenant’s missing notification target, while preserving the existing
empty-transaction and return behavior.
- Around line 27-33: Update the role predicate in the TenantUser query to
compare a lowercased TenantUser.role against the canonical lowercase values
“admin” and “owner”, replacing the explicit case variants while preserving the
active-user and tenant filters.
- Line 58: Update the persistence flow that builds the notification payload so
the ORM-assigned created_at value from TimestampMixin is available before
constructing NotificationDTO; flush the session and read the entity’s populated
created_at, or omit created_at from the listener payload instead of sending
None. Preserve timestamps for live notifications consistently with REST
responses.
- Around line 48-63: Update the notification loop in the outbound persistence
method to call PostgreSQL’s pg_notify with a bound parameter instead of
interpolating safe_payload into a NOTIFY statement, removing the manual quote
escaping. Keep the payload limited to notification identifiers such as
tenant_id, user_id, and id so it remains below PostgreSQL’s 8000-byte limit
while listeners retrieve the full row separately.

In
`@core/platform/apps/notification_engine/src/notification_engine/adapters/outbound/postgres_notification_query_repository.py`:
- Around line 34-43: Standardize NotificationDTO.created_at on one type across
REST and SSE by declaring the chosen type in the notification query port and
converting the value in the repository method that builds NotificationDTO
instances. Update
core/platform/apps/notification_engine/src/notification_engine/adapters/outbound/postgres_notification_query_repository.py
lines 34-43 accordingly, and construct the DTO with that same chosen type in
core/platform/apps/notification_engine/tests/unit/test_stream_manager.py lines
25-27.
- Around line 18-20: Update get_in_app_notifications to clamp the incoming limit
to an established safe maximum before passing it to the query’s .limit() call,
while preserving the existing default and result behavior for valid smaller
values.

In
`@core/platform/apps/notification_engine/src/notification_engine/adapters/outbound/postgres_route_repository.py`:
- Around line 62-79: Move the mapping returns inside their transaction/session
scopes so ORM attributes are accessed before detachment: in
core/platform/apps/notification_engine/src/notification_engine/adapters/outbound/postgres_route_repository.py#L62-L79,
return self._map_preference(row) within the async with block; in
core/platform/apps/notification_engine/src/notification_engine/adapters/outbound/postgres_template_repository.py#L76-L100,
return self._map_template(row) within the async with block. If the session
factory uses expire_on_commit=False, document that in a comment, but still keep
both mappings inside their respective blocks.
- Around line 25-41: Update get_channels to handle invalid stored channel values
individually: catch the Channel(c) conversion failure, skip that value, and log
the unknown channel with the repository’s existing logger. Preserve valid
channels and the empty-list behavior when no route exists, ensuring one invalid
value does not fail the tenant’s dispatch read path.

In
`@core/platform/apps/notification_engine/src/notification_engine/adapters/outbound/postgres_template_repository.py`:
- Around line 30-34: Define a shared named constant for the platform sentinel
tenant value "ten_000" in the appropriate common module, then import and use it
in the tenant lookup loop of the template repository instead of the hardcoded
literal. Update other platform-default seeding or querying modules to reuse the
same constant.

In
`@core/platform/apps/notification_engine/src/notification_engine/adapters/outbound/template_renderer.py`:
- Around line 8-22: Update _SilentUndefined to inherit from Jinja’s
ChainableUndefined, or implement __getattr__ and __getitem__ to return self,
while preserving its existing empty-string, empty-iteration, and false-boolean
behavior so chained missing attributes and items render safely.

In
`@core/platform/apps/notification_engine/src/notification_engine/api/in_app_notifications_router.py`:
- Around line 20-65: Share the tenant authorization logic by moving
_assert_tenant_authorized from templates_router.py into a shared module,
treating None authorized_tenants as an empty set, and importing it in
templates_router.py instead of defining it locally. In
in_app_notifications_router.py, invoke the shared helper at the start of
get_user_notifications, mark_notification_read, and stream_notifications, and
validate that the authenticated identity matches the user_id path parameter for
each endpoint.
- Around line 20-31: Protect all three tenant/user-scoped
handlers—get_user_notifications, mark_notification_read, and
stream_notifications—by validating both tenant_id and user_id before accessing
notification data, updating read status, or opening the SSE stream. Extract the
existing _assert_tenant_authorized helper from templates_router.py into a shared
authorization module, then reuse it in both routers and apply the corresponding
user authorization check at the start of each handler.
- Around line 67-89: Update the notification stream’s event_generator to
unsubscribe tenant_id, user_id, and queue in a finally block so cleanup runs for
cancellation and all other exits; retain cancellation propagation without
duplicating cleanup. Add a periodic SSE heartbeat comment while the stream is
idle, and constrain the endpoint’s limit query parameter with Query(50, ge=1,
le=200).

In
`@core/platform/apps/notification_engine/src/notification_engine/api/router.py`:
- Around line 14-16: Add a separate readiness handler alongside health_check
that verifies database connectivity, the Postgres listener, and the SQS consumer
before returning success; report an appropriate non-success response when any
dependency is unavailable, while keeping health_check as the lightweight
liveness endpoint.

In
`@core/platform/apps/notification_engine/src/notification_engine/api/templates_router.py`:
- Around line 99-116: Move _assert_tenant_authorized from templates_router.py
into a shared authorization module, preserving its authentication and
tenant-membership checks and HTTP errors. Update templates_router.py,
preferences_router.py, and in_app_notifications_router.py to import and reuse
the shared helper, removing their duplicate implementations.
- Around line 108-116: Normalize identity.authorized_tenants to an empty set
when the attribute is missing or None before the membership check in the
template authorization flow. Update the authorized assignment near the identity
validation so tenant_id not in authorized consistently raises the existing 403
response instead of propagating a TypeError.
- Around line 185-215: Harden preview_template by enforcing maximum sizes for
body_template and mock_payload before rendering, using the project’s existing
validation conventions or explicit limits. Replace both synchronous
renderer.render calls with asyncio.to_thread wrapped in asyncio.wait_for using a
2-second timeout, while preserving the current 400 response handling for render
failures and timeouts.
- Around line 211-215: Update the template-rendering exception handling to catch
only the relevant Jinja2 render/templating exception types instead of broad
Exception. Log the full exception server-side, then raise HTTPException with a
generic client-safe 400 detail that does not include exc, preserving exception
chaining where appropriate.

In
`@core/platform/apps/notification_engine/src/notification_engine/application/consumer.py`:
- Around line 79-88: Make Consumer.stop asynchronous and await the current _task
after setting _shutdown_event, clearing _task only once _run() has exited.
Update the lifespan shutdown caller to await stop(), preserving start()’s
existing task-creation behavior.

In
`@core/platform/apps/notification_engine/src/notification_engine/application/stream_manager.py`:
- Around line 21-24: Update subscribe in StreamManager to create each client
asyncio.Queue with an explicit finite maxsize. In the notification
broadcast/delivery flow around the referenced queue handling, define the
full-queue policy by dropping the live notification when put_nowait encounters a
full queue, while preserving registration and allowing clients to recover
through the notification query endpoint.

In
`@core/platform/apps/notification_engine/src/notification_engine/bootstrap/lifespan.py`:
- Around line 28-31: Update the DATABASE_URL validation in the lifespan startup
flow to raise a configuration error when the variable is missing instead of
logging a warning and returning. Ensure startup fails before route registration
or listener initialization proceeds, while preserving normal initialization when
DATABASE_URL is present.
- Around line 48-51: Update the lifespan teardown around the module-level
_container by tracking whether startup completed successfully, and skip all
teardown when startup returned early before initialization. When startup did
complete, stop the postgres listener and shut down resources in separate
protected cleanup steps so a listener.stop() failure cannot prevent
_container.shutdown_resources() from running.

In
`@core/platform/apps/notification_engine/src/notification_engine/workers/consumer.py`:
- Around line 35-62: Wrap the post-initialization worker lifecycle in a
try/finally block, using the code after container.init_resources() through
shutdown handling as the try body and ensuring container.shutdown_resources()
executes in finally. Preserve the existing signal handling, consumer stopping,
timeout warning, and successful shutdown log while guaranteeing resource cleanup
when any intermediate operation fails.
- Around line 18-21: In
core/platform/apps/notification_engine/src/notification_engine/workers/consumer.py:18-21,
extract the shared bootstrap into a helper that upward-searches for .env,
validates NOTIFICATION_QUEUE_URL and NOTIF_OUTBOX_POLL_INTERVAL, initializes
resources, and always releases container resources in a finally block; have
run_consumer pass container.consumer_worker() to it. In
core/platform/apps/notification_engine/src/notification_engine/workers/sweeper.py:18-21,
remove the duplicated bootstrap and have run_sweeper call the shared helper with
container.sweeper_worker().
- Around line 18-21: Replace the fixed-depth dotenv_path construction before
load_dotenv with upward search from __file__ for the nearest .env, or use a
configured environment-variable path when provided. Pass the resolved path to
load_dotenv and preserve the worker’s existing configuration behavior when no
.env is found.
- Around line 30-33: Validate the environment values before configuring the
container: require NOTIFICATION_QUEUE_URL to be non-empty and raise a clear
startup error when missing, and parse NOTIF_OUTBOX_POLL_INTERVAL with explicit
ValueError handling that reports the invalid value clearly. Update the
configuration flow around container.config.sqs_queue_url.from_value and
container.config.poll_interval.from_value while preserving valid interval
behavior.

In
`@core/platform/apps/notification_engine/tests/api/test_in_app_notifications_router.py`:
- Around line 69-94: Add a cross-tenant authorization scenario to the in-app
notifications router test, using the existing AsyncClient flow and
authentication setup to request tenant B’s notification path while authenticated
as tenant A. Assert that fetching or marking the other tenant’s notification
returns 403 or 404, while preserving the existing same-tenant assertions.

In `@core/platform/apps/notification_engine/tests/conftest.py`:
- Around line 27-47: The function-scoped db_engine fixture currently drops and
recreates all GlobalRegistry.metadata tables for every test. Move schema
creation and metadata initialization into a session-scoped fixture, then isolate
individual tests using transaction rollback or targeted TRUNCATE ... CASCADE
cleanup while preserving the existing engine lifecycle and schema setup.
- Around line 52-62: Rewrite the async_sessionmaker fixture docstring to state
the actual test isolation strategy directly: db_engine drops and recreates the
schema for each test function, providing a fresh database. Remove the
exploratory alternatives, contradictions, and train-of-thought text.
- Around line 14-18: Remove the custom event_loop fixture from conftest.py and
rely on pytest-asyncio’s provided loop. Configure
asyncio_default_fixture_loop_scope = "function" under [tool.pytest.ini_options],
or set loop_scope explicitly only where required.

In
`@core/platform/apps/notification_engine/tests/integration/test_integration_outbox_sweeper.py`:
- Around line 62-67: Update the integration test around
NotificationOutboxSweeper.poll to verify that poll is the public one-cycle API,
not an internal loop helper, and explicitly assert the claimed lease and
expected sweeper outcome after the cycle. Keep poll_interval_seconds=0 and the
direct single-cycle invocation, but make the test fail if the lease or contract
is not satisfied.
- Around line 44-60: Insert a valid Tenant row for tenant_id "t1" before calling
repo.save(message) in the outbox sweeper test. Follow the tenant setup pattern
used by sibling integration tests, ensuring the row is persisted through the
existing database session or fixture so the foreign-key constraint is satisfied.

In
`@core/platform/apps/notification_engine/tests/integration/test_integration_postgres_listener.py`:
- Around line 33-36: Replace the fixed sleeps in the integration test with
deadline-based condition waits: add a readiness signal to
PostgresNotificationListener that is set after ADD_LISTENER succeeds, await it
before issuing NOTIFY, and poll stream_manager.broadcasts until the expected
notification arrives instead of sleeping for delivery. Keep timeout failures
explicit and bounded.
- Around line 62-71: Ensure cleanup runs regardless of assertion outcomes: in
core/platform/apps/notification_engine/tests/integration/test_integration_postgres_listener.py#L62-L71,
wrap the assertions using listener in try/finally and always await
listener.stop(); in
core/platform/apps/notification_engine/tests/api/test_in_app_notifications_router.py#L65-L67,
similarly guarantee container.unwire() and reset_override() execute in finally,
or move both resource lifecycles into pytest fixtures with teardown.
- Around line 51-56: Replace the f-string SQL in the integration test’s database
notification block with PostgreSQL’s pg_notify function using bound parameters
for both the channel and payload. Remove the inaccurate escaping/injection
comments and preserve the existing in_app_notifications channel and payload
behavior.

In
`@core/platform/apps/notification_engine/tests/integration/test_postgres_in_app_persistence.py`:
- Around line 30-35: Add an integration test alongside the existing
repo.save_notification case that omits target_user_id from the data dictionary,
then assert the intended behavior for a missing recipient, including the
expected result or exception. Also cover a nonexistent target_user_id if that
behavior is part of the adapter contract, using the existing test fixtures and
assertion conventions.

In
`@core/platform/apps/notification_engine/tests/integration/test_postgres_notification_query_repository.py`:
- Around line 54-64: Extend the mark_as_read integration test to verify tenant
and user scoping, not just an unknown notification ID. Using mark_as_read,
attempt to update an existing notification with an incorrect tenant_id and with
an incorrect user_id, and assert both calls return False while the notification
remains unread for its rightful scope.

In
`@core/platform/apps/notification_engine/tests/integration/test_postgres_outbox_repository.py`:
- Around line 36-49: The integration test around claim_next_messages must verify
worker exclusivity and lease expiry, not only completion. Add an immediate claim
using a different worker_id and assert it returns no events; then create or
retain a pending row with a short lock_lease_ms, wait for the lease to expire,
claim it with the second worker, and assert that row is returned.
- Around line 36-37: Update the assertion following repo.claim_next_messages to
require exactly one returned event instead of accepting any nonempty result.
Preserve the existing claim_next_messages invocation and verify the result
length is precisely 1.

In
`@core/platform/apps/notification_engine/tests/integration/test_postgres_route_repository.py`:
- Around line 25-35: Add an integration test for
PostgresRouteRepository.get_channels using a route containing an unrecognized
channel string alongside valid channels. Assert the unknown value is skipped,
valid channels are returned, and a warning is logged; do not allow Channel
conversion to propagate ValueError.

In
`@core/platform/apps/notification_engine/tests/integration/test_postgres_template_repository.py`:
- Around line 49-60: Extend the integration coverage around get_template to seed
a template for the ten_000 sentinel tenant and verify a tenant without its own
template receives that fallback. Add a separate assertion showing that when both
templates exist, the tenant-specific template is returned instead of the ten_000
fallback.

In
`@core/platform/apps/notification_engine/tests/unit/test_delivery_dispatcher.py`:
- Around line 40-49: Update test_delivery_dispatcher_missing_strategy to retain
references to all FakeDeliveryStrategy instances and explicitly assert that none
recorded a delivery after dispatching Channel.EMAIL with its strategy removed.
If dispatch emits a warning for the missing channel, capture it with caplog and
assert the expected warning as well.

In `@core/platform/apps/notification_engine/tests/unit/test_dispatch_use_case.py`:
- Around line 102-111: Update test_dispatch_no_routes to bind the FakeOutboxRepo
instance to a local variable before constructing DispatchNotificationUseCase,
pass that variable into the use case, and assert its events collection directly
instead of accessing uc.outbox_repo.
- Around line 97-99: Add a unit test alongside the existing idempotency
assertion in the dispatch use case tests for two otherwise distinct events with
no event_id. Verify the missing-ID behavior produces distinct idempotency keys
and allows both notifications to be persisted, rather than reusing the
fallback-derived key and triggering the unique outbox constraint.

In `@core/platform/apps/notification_engine/tests/unit/test_outbox_sweeper.py`:
- Around line 76-86: Remove the unused repo.swept setup from the test, or update
the fake repository to record sweep_stuck_messages and assert that poll invokes
it with the expected value. Keep the existing dispatch and completion assertions
unchanged.

In `@core/platform/apps/notification_engine/tests/unit/test_postgres_listener.py`:
- Line 22: Update the lifecycle test for PostgresNotificationListener to verify
DSN normalization: retain the asyncpg DSN input and assert mock_connect was
called once with the corresponding postgresql://foo DSN, rather than checking
only the call count.

In `@core/platform/apps/notification_engine/tests/unit/test_sqs_poller.py`:
- Around line 47-53: Remove the unnecessary try/except asyncio.CancelledError
wrapper around the poll_sqs_queue task cancellation and gather flow, since
gather(..., return_exceptions=True) absorbs cancellation as a result; apply the
same cleanup to the duplicate pattern near the second test block.
- Around line 66-100: Update fake_processor and the test assertions around
poll_sqs_queue to record processor invocations, then assert the invocation count
remains zero after processing the invalid JSON message. Preserve the existing
delete_message assertion and cancellation flow.

In `@core/platform/apps/notification_engine/tests/unit/test_stream_manager.py`:
- Around line 42-46: Extend the cleanup assertions in the test around
StreamManager.unsubscribe to also verify that the (tenant_id, "user-2") key is
absent from manager._queues after removing queue3, while preserving the existing
assertion for user_id.

In `@core/platform/packages/identity/src/identity/domain/identity_context.py`:
- Line 90: Update identity_context_from_claims to pass the trusted tenant
mapping when constructing IdentityContext, instead of relying on
tenant_mapping’s empty default. Preserve the existing mapping validation in the
IdentityContext construction path so platform administrators with required
organization mappings are accepted.
- Around line 40-48: Update identity_context.py at lines 40-48 and 101-108: in
both project-specific Zitadel roles claim loops, aggregate or evaluate every
matching claim instead of stopping at the first match. Use the generic roles
claim only when no project-specific claim exists, while preserving tenant
allocation merging and platform-admin role evaluation across all matching
project claims.

In
`@core/platform/packages/orm/migrations/versions/843b3e309769_initial_schema.py`:
- Around line 334-340: Update the ix_in_app_notif_tenant_user_read definition in
core/platform/packages/orm/migrations/versions/843b3e309769_initial_schema.py:334-340
and the matching Index(...) in
core/platform/packages/orm/src/platform_orm/models/notifications.py:88-91 to
append created_at after is_read, keeping both definitions identical.
- Around line 346-348: Update the channels column definition to use an explicit
JSONB server-default expression via sa.text(...), including the appropriate
JSONB cast, instead of the bare string server_default="[]".
- Around line 285-286: Add a follow-up Alembic revision after 843b3e309769 that
adds the notification_templates created_at and updated_at columns and creates
all new notification tables introduced by this change. Ensure its upgrade
applies these changes to existing environments and its downgrade reverses them.

In `@core/platform/packages/orm/src/platform_orm/models/notifications.py`:
- Line 65: Update the destination_config annotation on the notifications model
to represent nullable values, using an optional mapping type while keeping the
JSONB column nullable. Ensure type checking requires callers to handle None
before indexing.

In `@core/ucp/apps/dashboard/src/components/layout/AppLayout.tsx`:
- Around line 35-39: Update AppLayout to conditionally render the sticky header
element only when the optional headerContent value is present, while keeping the
children container and existing header styling unchanged.

In `@core/ucp/apps/dashboard/src/components/layout/TenantProvider.tsx`:
- Around line 25-39: Update TenantProvider’s missing-tenant branch to avoid
throwing during render, including when auth.isLoading is false but auth.user is
undefined. Render an explicit error state and log the diagnostic details through
the existing logging mechanism, keeping profile claim names out of exception
messages and preserving the loading behavior.

In
`@core/ucp/apps/dashboard/src/routes/_authenticated/tenant/notifications/index.tsx`:
- Around line 10-24: Update NotificationsRoute to destructure token from
useTenantContext() and pass that context token to NotificationsPage; remove
auth.user.access_token from the guard and props while retaining useAuth() only
for profile.sub and preserving the existing tenantId validation.

In
`@core/ucp/packages/ucp/src/ucp/adapters/inbound/http/middleware/authentication.py`:
- Around line 67-68: Remove the token query-parameter branch from the
authentication middleware, including the logic around token in
request.query_params. Authenticate SSE requests through the established
authenticated-cookie mechanism, or require redemption of a short-lived
single-use stream ticket without accepting reusable bearer tokens in the URL.

In `@pyproject.toml`:
- Around line 95-96: Update the Ruff lint configuration by enabling the “S”
security rule set in the active select list, or alternatively document and
enforce an equivalent security-lint CI job before merge; ensure the notification
engine retains an active security gate.
- Around line 110-111: Remove the tree-wide B008 ignores for the
notification_engine and unified-api source globs in pyproject.toml, then
annotate only intentional Depends(...) default arguments with # noqa: B008 or
configure an equivalently narrow scope. Preserve B008 reporting for all
unrelated default-argument calls.

In `@TECHNICAL_DEBT.md`:
- Around line 97-101: Update the Tailwind-related action item in
TECHNICAL_DEBT.md after checking the repository’s declared Tailwind version.
Replace the unsupported blanket “disable arbitrary values” configuration step
with an enforceable ESLint rule or class/token validator that rejects
unauthorized arbitrary values, while preserving the intended design-system
constraint.
- Around line 75-79: The Notifications In-App Notification Delivery UX debt item
should no longer claim SSE delivery or tenant-scoped event_type routing are
missing. Update the item to focus on user-scoped subscription preferences: add
user-level opt-in/opt-out controls and apply those preferences during recipient
and channel resolution for In-App versus Email alerts.

In `@tests/architecture/test_boundaries.py`:
- Around line 10-24: In test_domain_layer_isolation, remove the pytest skip
decorators and configure the architecture check against the notification_engine
package root. Update the domain matcher and forbidden imports to
fnmatch-compatible patterns: notification_engine.domain*,
notification_engine.adapters*, notification_engine.api*, sqlalchemy*, fastapi*,
and asyncpg*.
