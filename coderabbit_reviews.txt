In
`@apps/edi/packages/edi/src/edi/adapters/outbound/database/postgres_data_plane_outbox_repository.py`:
- Line 186: The delivery claim flow in claim_delivery_outbox_event must use a
status distinct from the normal PROCESSING state, such as DELIVERING; add that
value to OutboxStatus and update related status handling so sweep_stuck_events
and claim_next_events do not reset or reclaim delivery-owned rows. Ensure the
delivery claim updates updated_at consistently, preserving
mark_delivery_success’s ability to record the completed delivery.

In `@core/platform/packages/database/src/database/models/identity.py`:
- Around line 29-32: Add a follow-up database migration for the existing table
created by adee9680c2ed_initial_schema.py: normalize or validate existing status
values using LifecycleStatus, then add the ck_tenants_status constraint. Ensure
the migration is reversible and leaves existing valid active/inactive rows
unchanged.

Apply the same fix in
`@core/platform/packages/database/migrations/versions/adee9680c2ed_initial_schema.py`
at line 73: The same missing forward-migration issue applies to databases that
already ran the initial revision.

In
`@core/platform/packages/identity/src/identity/adapters/outbound/database/user_repository.py`:
- Line 61: Update the user status handling around UserRepository’s
UserStatus(db_user.status) conversion to safely handle persisted values outside
the supported “active” and “inactive” statuses, or enforce those values with a
model/database constraint and migration. Ensure invalid existing rows cannot
cause every lookup to raise an unhandled ValueError.

In `@package.json`:
- Around line 20-25: Update the five database and infrastructure scripts using
“source ../../../../.env” to POSIX-compatible environment-file loading with “.
../../../../.env”, preserving the existing directory changes, environment export
toggling, and commands.

`@apps/edi/apps/edi-background-worker/tests/integration/test_sweeper_integration.py`:
- Around line 20-22: Define and expose a pytest db_router fixture for the
background-worker integration tests so test_sweeper_fetches_and_processes_events
can receive it and call DatabaseRouter.get_shard_session successfully. Reuse the
established test database/router setup and lifecycle, keeping the fixture scoped
to the background-worker tests rather than relying on the unrelated config-sync
test_db_router fixture.
