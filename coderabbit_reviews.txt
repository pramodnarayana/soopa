In
`@apps/edi/packages/ui/src/features/partners/components/AS2PartnershipDetails.tsx`:
- Around line 47-74: Synchronize the form when as2Partnership changes by adding
a useEffect near the existing mdn defaulting effect that calls reset with the
current partnership field values and the same fallbacks used in defaultValues.
Include as2Partnership in the effect dependencies, preserve the existing
mdnType/mdnUrl behavior, and avoid marking the reset form dirty.

In
`@apps/edi/packages/ui/src/features/partners/components/CreateAS2PartnershipModal.tsx`:
- Around line 55-75: Update handleSave to trim the name before building the
createPartnership.mutate payload, matching the guard used by
CreateApiTokenDialog; submit undefined when the trimmed value is empty and the
trimmed name otherwise.

In
`@core/platform/packages/identity/node/src/adapters/outbound/zitadel/ZitadelJwksVerifier.ts`:
- Around line 67-99: Update the userinfo cache logic in the token verification
flow to use a stable fallback key when claims.jti is absent, such as a hash
derived from token, and use that key consistently for lookup and storage in
userinfoCache. Preserve the existing jti-based behavior and TTL/eviction
handling.

In
`@core/ucp/apps/ucp-api/src/adapters/inbound/http/guards/platform-auth.guard.ts`:
- Around line 39-45: Update the catch block in the platform authentication guard
to log only the generic “Authentication failed” message, removing the caught
error object from this.logger.error while preserving the existing
ForbiddenException rethrow and UnauthorizedException response.

In
`@core/ucp/apps/ucp-api/src/adapters/inbound/http/zitadel-webhook.controller.ts`:
- Around line 95-107: Update the new-user ID fallback in the webhook handler to
use generateId('usr') from `@soopa/database` instead of createId(). Preserve
existingUser?.id for existing users and remove the now-unused createId import if
applicable.

In
`@core/ucp/apps/ucp-api/src/adapters/outbound/database/tenant.drizzle.repository.ts`:
- Around line 38-78: Extract the duplicated subscription query from findById and
findByIdpTenantId into a private loadSubscriptionSlugs(tenantId: string) helper
that returns the mapped app slugs. Replace both inline query blocks with calls
to this helper while preserving each method’s existing lookup and mapToDomain
behavior.
- Around line 179-198: Move tenant.clearEvents() out of the db.transaction
callback in the repository save flow, placing it only after the transaction
promise resolves successfully. Preserve event data when commit fails so retrying
save(tenant) persists the outbox events, matching WebhookDrizzleRepository.save
behavior.

In `@core/ucp/apps/ucp-api/src/app.module.ts`:
- Around line 108-146: Validate ZITADEL_UCP_PROJECT_ID once during configuration
bootstrap rather than repeating the presence check in the ZitadelJwksVerifier
and AuthenticateUseCase factories. Add the validation to ConfigModule.forRoot({
validate }) or extract a shared requireProjectId helper and reuse it from both
factories, preserving the existing error message and returned project ID.

`@core/ucp/apps/ucp-api/src/adapters/outbound/database/user.drizzle.repository.ts`:
- Around line 93-110: Update deleteOrphanedUsers to perform one atomic delete
guarded by a correlated NOT EXISTS subquery against tenantUsers, rather than
selecting assignments and filtering IDs in memory; import and use notExists and
sql from drizzle-orm as needed, while retaining the empty userIds early return.