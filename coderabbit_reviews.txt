In `@apps/platform-dashboard/src/routes/_authenticated.tsx`:
- Around line 36-38: Update the internal Dashboard and Tenants navigation
elements in the authenticated route component to use TanStack Router’s Link
component, importing it as needed so registered routes avoid full page reloads.
Keep the Scheduler navigation as a native anchor until that route is registered.

In `@apps/platform-dashboard/src/routes/_authenticated/tenants.tsx`:
- Line 51: Update the DialogTrigger usage in the tenant provisioning UI to use
the supported asChild composition pattern instead of the invalid render prop,
while preserving the existing Button trigger and “Provision Tenant” label.
- Line 109: Replace the explicit any annotation in the tenants.map callback with
a specific inline object type or a named tenant interface matching the tenant
data used by the route, while preserving the existing mapping behavior and
property access.

In `@packages/identity/python/.coverage`:
- Around line 52-65: Remove the tracked .coverage artifact from the repository
and add .coverage to the project’s .gitignore so locally generated coverage
databases are ignored in future commits.

In `@packages/identity/python/soopa_identity/domain/identity_context.py`:
- Around line 17-25: Update the parse_roles validator to return an empty list
when v is None, before the existing dict, list, and scalar handling, while
preserving all other role-conversion behavior.

In `@server/ucp-api/src/adapters/inbound/http/api-keys.controller.ts`:
- Line 14: Replace the TypeScript utility type in the controller’s `@Body`()
parameter with a runtime DTO class, using a dedicated request DTO or NestJS
OmitType based on GenerateApiKeyDto while excluding tenantId. Ensure the
resulting class preserves validation metadata so ValidationPipe can instantiate
and validate the request body.

In `@server/ucp-api/src/adapters/outbound/database/outbox.drizzle.repository.ts`:
- Around line 40-45: Update markAsFailed to accept the upstream errorMessage as
a second argument and include it in the controlPlaneOutbox update alongside
status and updatedAt. Remove the outdated comment and ensure the passed error
reason is persisted to the errorReason column.

In `@server/ucp-api/src/main.ts`:
- Line 6: Update the CORS configuration in the application bootstrap around
app.enableCors() to explicitly set the origin option to the trusted platform
dashboard domain or configured allowlist, rather than permitting all origins by
default.

In `@apps/platform-dashboard/`@/lib/utils.ts:
- Around line 1-7: Remove the generated literal “@” directory and relocate or
recreate its utils file under the established src/lib location. Update
components.json path mappings so aliases resolve to src/ for library and
component paths, then ensure generators and imports rely only on src/lib and
src/components.

In `@docker-compose.yml`:
- Around line 34-46: Update the ucp_engine service’s depends_on configuration to
include localstack with a service_started condition, while preserving the
existing ucp_postgres health dependency.

In `@server/ucp-api/eslint.config.mjs`:
- Around line 29-35: Update the TypeScript ESLint rule configuration to enforce
strict typing: remove the explicit-any override so
`@typescript-eslint/no-explicit-any` uses its default strict setting, and change
each listed `@typescript-eslint/no-unsafe-`* rule from warn to error. Keep
unrelated ESLint rules unchanged.

In `@server/ucp-api/README.md`:
- Around line 24-26: Replace the default NestJS scaffolding content in the
README’s Description section with project-specific documentation for the UCP
API, including its purpose, setup instructions, and role within the broader
platform. Remove references to the generic Nest starter repository and preserve
the README’s existing structure where applicable.

In `@server/ucp-api/src/adapters/inbound/http/api-key.guard.ts`:
- Line 31: Trim the secret extracted in the API key guard after removing the
Bearer prefix. Update the rawSecret assignment around substring(7) to apply
trim(), preserving the existing header parsing flow while removing leading or
trailing whitespace from the token.

In `@server/ucp-api/src/application/services/outbox-sweeper.service.ts`:
- Around line 25-27: Prevent duplicate event processing when multiple
OutboxSweeperService instances run concurrently by adding atomic claim/locking
behavior to fetchPendingEvents. Update the outbox repository query to use
row-level locking such as FOR UPDATE SKIP LOCKED, or the existing
distributed-locking mechanism, so each fetched event is exclusively claimed
before processing while preserving the batch limit of 50.

In `@server/ucp-api/src/application/use-cases/provision-tenant.use-case.ts`:
- Around line 9-13: Add class-validator decorators to ProvisionTenantDto in
server/ucp-api/src/application/use-cases/provision-tenant.use-case.ts:9-13,
validating name and adminEmail as non-empty strings and appSlugs as a non-empty
array of strings. Add corresponding decorators to GenerateApiKeyDto in
server/ucp-api/src/application/use-cases/generate-api-key.use-case.ts:8-12,
validating tenantId, name, and scopes with appropriate non-empty string/array
constraints.

In `@server/ucp-api/src/ports/outbound/message.bus.ts`:
- Line 1: Update the MESSAGE_BUS dependency-injection token to use a Symbol,
matching the existing IDENTITY_PROVIDER and API_KEY_REPOSITORY token conventions
while preserving its exported name and usage.