[project]
name = "pykcs11"
version = "1.5.18"
description = "Dummy package to satisfy endesive requirement without compiling C extension"
requires-python = ">=3.11"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["PyKCS11"]
