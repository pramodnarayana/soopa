{
  "name": "@soopa/identity-outbox-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Worker service for handling Identity outbox relay and sweeping",
  "scripts": {
    "start": "PYTHONUNBUFFERED=1 uv run python -m identity_outbox_worker.main",
    "dev": "PYTHONUNBUFFERED=1 uv run python -m identity_outbox_worker.main"
  }
}
