{
  "name": "@soopa/identity-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "uv run python -m identity_worker.main",
    "dev": "PYTHONPATH=src uv run python -m identity_worker.main"
  }
}
