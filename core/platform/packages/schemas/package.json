{
  "name": "@soopa/schemas",
  "version": "1.0.0",
  "private": true,
  "description": "Shared JSON Schemas and Contracts",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "generate": "pnpm generate:ts && pnpm generate:py",
    "generate:ts": "json2ts -i 'events/*.json' -o src/generated --cwd events && sed -i '' 's/export const enum/export enum/g' src/generated/*.ts",
    "generate:py": "mkdir -p python && uv run --with datamodel-code-generator datamodel-codegen --input events --output python --input-file-type jsonschema"
  },
  "devDependencies": {
    "json-schema-to-typescript": "^15.0.3"
  }
}
