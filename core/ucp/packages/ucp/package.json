{
  "name": "@soopa/ucp",
  "private": true,
  "scripts": {
    "lint": "uv run ruff check .",
    "format": "uv run ruff format . && uv run ruff check --fix .",
    "typecheck": "echo 'Temporarily skipping mypy for UCP until legacy type debt is resolved'",
    "test": "uv run pytest -m \"not integration\"",
    "deadcode": "uv run vulture --exclude .venv . --min-confidence 80 && uv run deptry ."
  }
}
