{
  "name": "@soopa/edi-secrets-sidecar",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "typecheck": "MYPYPATH=. uv run mypy --explicit-package-bases main.py",
    "cov": "uv run pytest -m \"not integration\" --cov=src --cov-report=term-missing --ignore=tests/integration --ignore=tests/e2e"
  },
  "devDependencies": {
    "@soopa/api": "workspace:*"
  }
}
